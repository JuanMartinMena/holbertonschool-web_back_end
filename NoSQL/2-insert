// Insert a document with attribute name "Holberton school"
result = db.school.insertOne({
  name: "Holberton school"
});

// Mimic the WriteResult output format
print("WriteResult({ \"nInserted\" : " + result.insertedCount + " })");
